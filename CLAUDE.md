## Git conventions
- Commit after completing each logical section of work (e.g. after HTML structure is done, after CSS is done, after JS is done, after images are extracted and placed).
- Use conventional commit messages: feat:, fix:, style:, refactor:
- Do not push to remote unless explicitly asked.
- Always run git add -A before committing.
